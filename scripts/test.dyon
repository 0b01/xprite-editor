fn main() {
    lib :=
    println(lib)
    ~xpr := {to_draw: []}

    // w := rand(100, 150)
    // h := rand(100, 150)
    // draw_block((-w / 2, -h / 2), (w / 2, h / 2));

    // rect(0, 0, 10, 10, #000000)
    println(xpr)
}

// fn draw_block(v1, v2) {
//     if (v2.x - v1.x < cutoff || v2.y - v1.y < cutoff) {
//         draw_rectangle(v1, v2)
//         return
//     }
//     let decide = random()

//     if (decide < 0.4) {
//         draw_rectangle(v1, v2);
//         draw_section(p.createVector(v1.x + padding, v1.y + padding), p.createVector(v2.x - padding, v2.y - padding));
//     } else if (decide < 0.95) {
//         split_block(v1, v2);
//     } else {
//         draw_rectangle(v1, v2);
//     }
// }

